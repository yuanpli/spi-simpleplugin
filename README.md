# JAVA sample plugin with SPI and SpringBoot

## Running

Build this project by executing `./mvnw install`, 
then in the `target` dir of `distribution` module which contains a launch script.
> bin/launcher.sh

